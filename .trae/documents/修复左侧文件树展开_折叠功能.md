## 问题分析

1. **文件夹图标问题**：ChevronRight图标始终显示，没有根据折叠状态旋转
2. **点击交互问题**：点击文件夹名称时，只设置了selectedFolder，没有触发折叠状态切换
3. **布局结构问题**：文件树的层级关系显示不正确

## 修复方案

### 1. 修复文件夹图标旋转问题
- 在`file-manager.tsx`中，更新Tree组件的Collapsible配置，确保ChevronRight图标根据折叠状态旋转

### 2. 修复点击交互逻辑
- 在`folder-item/index.tsx`中，调整点击事件处理，确保点击文件夹时触发折叠状态切换

### 3. 优化文件树布局
- 确保文件树的层级关系正确显示
- 修复文件夹和文件的缩进问题

## 实现步骤

1. **修改`file-manager.tsx`**：
   - 更新Collapsible组件的className，确保图标正确旋转

2. **修改`folder-item/index.tsx`**：
   - 调整点击事件处理，确保点击文件夹时触发折叠状态切换
   - 修复ChevronRight图标的显示逻辑

3. **测试修复效果**：
   - 确保文件夹可以正常展开和折叠
   - 确保文件夹图标正确显示折叠状态
   - 确保文件树层级关系正确

## 预期效果

- 文件夹可以通过点击正常展开和折叠
- 文件夹图标（ChevronRight）根据折叠状态正确旋转
- 文件树清晰显示文件夹和文件的层级关系
- 文件夹目录可以收起或展开，符合用户期望