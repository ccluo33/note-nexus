## 实现新建文件到正确位置

### 1. 分析当前新建文件逻辑
- **文件**：`src/stores/article.ts`
- **函数**：`newFile` 函数
- **当前逻辑**：
  - 如果 `activeFilePath` 包含 '/'，则在当前活动文件的父文件夹下创建新文件
  - 否则，在根目录下创建新文件
- **目标**：确保逻辑正确，无bug

### 2. 检查文件夹项的新建文件功能
- **文件**：`src/app/core/article/file/folder-item/index.tsx`
- **函数**：`handleNewFile` 函数
- **目标**：确保在当前文件夹下正确创建新文件

### 3. 检查文件管理器的新建文件功能
- **文件**：`src/app/core/article/file-manager.tsx`
- **目标**：确保文件管理器的新建文件功能正常

## 增强文件拖动功能

### 1. 修改文件管理器的拖放处理
- **文件**：`src/app/core/article/file/file-manager.tsx`
- **函数**：`handleDrop` 函数
- **修改点**：增强处理逻辑，支持将应用内文件拖动到其他文件夹
- **实现逻辑**：
  - 解析拖放事件，获取源文件路径和目标文件夹路径
  - 使用 `rename` 函数更新文件系统中的实际文件位置
  - 更新文件树中的文件路径和父节点关系
  - 重新加载文件树以反映更改

### 2. 修改文件夹项的拖放处理
- **文件**：`src/app/core/article/file/folder-item/index.tsx`
- **函数**：`handleDrop` 函数
- **修改点**：增强处理逻辑，支持接收拖放的文件
- **实现逻辑**：
  - 解析拖放事件，获取源文件路径
  - 构建目标文件夹路径
  - 使用 `rename` 函数移动文件
  - 更新文件树

### 3. 修改文件项的拖动处理
- **文件**：`src/app/core/article/file/file-item.tsx`
- **函数**：`handleDragStart` 函数
- **修改点**：确保正确设置拖动数据

## 实现细节

### 1. 拖放事件处理
- 拖动时设置：`e.dataTransfer.setData('text', path)`
- 拖放时获取：`const sourcePath = e.dataTransfer?.getData('text')`
- 目标文件夹路径：从当前拖放元素获取

### 2. 文件移动逻辑
- 使用 `rename` 函数移动文件系统中的实际文件
- 更新文件树中的文件路径和父节点关系
- 重新加载文件树以反映更改

### 3. 错误处理
- 添加适当的错误处理，确保拖放操作安全可靠
- 处理文件已存在的情况
- 处理权限问题

## 测试计划

1. **新建文件位置测试**：
   - 打开A文件夹下的文件，新建文件，验证出现在A文件夹下
   - 选择B文件夹，新建文件，验证出现在B文件夹下
   - 测试根目录新建文件

2. **文件拖动测试**：
   - 将文件从A文件夹拖动到B文件夹，验证文件被正确移动
   - 将文件拖动到同一文件夹下，验证位置变化
   - 测试拖动多个文件

3. **边界情况测试**：
   - 测试拖动到不存在的目录
   - 测试拖动重名文件

通过以上修改，将实现用户需求：新建文件时，文件被创建在正确的位置（当前选择的文件夹或当前打开文件所在的文件夹），并支持将文件拖动到其他文件夹。