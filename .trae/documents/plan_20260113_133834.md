## 问题分析

通过分析代码，我发现用户提到的"上传文件或图片按钮"是`control-file.tsx`组件，它目前允许选择各种类型的文件，包括文本文件和代码文件。用户希望将其改为"上传图片"，并限制只能选择图片进行上传。

## 解决方案

修改`control-file.tsx`组件，将其功能改为只上传图片：

1. 更新文件过滤器，只允许选择图片类型
2. 更新按钮图标，改为图片相关图标
3. 更新按钮提示文本，改为"上传图片"
4. 重命名组件和相关函数，使其更符合图片上传的功能

## 实现步骤

1. 打开`d:\workspace\note-nexus\src\app\core\record\mark\control-file.tsx`文件
2. 修改文件过滤器，只允许选择图片类型
3. 更新按钮图标，改为`ImagePlus`
4. 更新按钮提示文本，改为"上传图片"
5. 重命名`selectFile`函数为`selectImages`
6. 重命名`readFileByPath`函数为`uploadImage`
7. 修改函数内部逻辑，处理图片上传
8. 更新组件名称为`ControlImageUpload`

## 预期效果

修改后，"上传文件或图片按钮"将变为"上传图片"按钮，只允许选择图片文件进行上传，按钮图标和提示文本也会相应更新，更符合图片上传的功能。