# 资源有限企业的文档与向量存储解决方案

## 1. 现有系统存储分析

### 1.1 文档存储
- **存储方式**：本地Markdown文件系统
- **访问方式**：通过文件系统API读写
- **优势**：简单直接，易于理解
- **劣势**：缺乏集中管理，不利于企业级协作和安全控制

### 1.2 向量存储
- **存储方式**：SQLite数据库
- **表结构**：包含文件名、分块ID、内容、向量嵌入等字段
- **相似度计算**：在应用层实现余弦相似度计算
- **优势**：轻量级，易于部署
- **劣势**：性能受限，不适合大规模向量数据

## 2. 资源有限企业的核心需求

- **轻量级部署**：减少服务器资源占用
- **集中化管理**：企业数据集中存储，便于管理和安全控制
- **低成本**：降低硬件和维护成本
- **高性能**：在有限资源下提供良好的查询性能
- **安全性**：确保企业数据安全

## 3. 轻量级企业存储架构设计

### 3.1 整体架构

```
+-------------------+     +-------------------+
|                   |     |                   |
|   Web客户端       |     |   管理后台        |
|  (Next.js)        |     |  (Next.js)        |
|                   |     |                   |
+-------------------+     +-------------------+
          |                         |
          +-------------------------+
                                      |
                                      v
+-------------------+     +-------------------+
|                   |     |                   |
|   应用服务器      |     |   API网关         |
|  (Node.js/Express)|     |  (Nginx)          |
|                   |     |                   |
+-------------------+     +-------------------+
          |
          v
+-------------------+     +-------------------+
|                   |     |                   |
|   主数据库        |     |   对象存储        |
|  (PostgreSQL)     |     |  (MinIO)          |
|                   |     |                   |
+-------------------+     +-------------------+
          |                         |
          +-------------------------+
                                      |
                                      v
+-------------------+     +-------------------+
|                   |     |                   |
|   向量数据库      |     |   缓存服务        |
|  (Qdrant/Lite)    |     |  (Redis)          |
|                   |     |                   |
+-------------------+     +-------------------+
```

### 3.2 核心组件说明

1. **Web客户端**：基于现有Next.js代码，移除桌面和移动端相关功能，专注于Web端使用
2. **管理后台**：简化版管理员控制台，用于用户管理和系统配置
3. **应用服务器**：Node.js/Express，处理业务逻辑
4. **API网关**：Nginx，提供反向代理和负载均衡
5. **主数据库**：PostgreSQL，存储用户、权限、文档元数据等
6. **对象存储**：MinIO，轻量级S3兼容存储，用于存储Markdown文档和附件
7. **向量数据库**：Qdrant Lite，轻量级向量数据库，用于存储和查询向量数据
8. **缓存服务**：Redis，提高查询性能

## 4. 文档资源存储解决方案

### 4.1 存储架构

- **文档元数据**：存储在PostgreSQL中，包含文件名、路径、大小、创建时间、更新时间、所有者、权限等信息
- **文档内容**：存储在MinIO对象存储中，以Markdown文件格式保存
- **附件**：与文档关联，存储在MinIO中，支持图片、PDF、Office文档等格式

### 4.2 数据库设计

**文档表（documents）**
```sql
CREATE TABLE documents (
    id SERIAL PRIMARY KEY,
    filename VARCHAR(255) NOT NULL,
    path VARCHAR(1024) NOT NULL,
    minio_key VARCHAR(1024) NOT NULL,
    size BIGINT NOT NULL,
    owner_id INTEGER NOT NULL REFERENCES users(id),
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
    is_deleted BOOLEAN NOT NULL DEFAULT FALSE,
    deleted_at TIMESTAMP
);
```

**文件夹表（folders）**
```sql
CREATE TABLE folders (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    path VARCHAR(1024) NOT NULL,
    parent_id INTEGER REFERENCES folders(id),
    owner_id INTEGER NOT NULL REFERENCES users(id),
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);
```

**文档权限表（document_permissions）**
```sql
CREATE TABLE document_permissions (
    id SERIAL PRIMARY KEY,
    document_id INTEGER NOT NULL REFERENCES documents(id),
    user_id INTEGER REFERENCES users(id),
    team_id INTEGER REFERENCES teams(id),
    permission_type VARCHAR(20) NOT NULL CHECK (permission_type IN ('read', 'write', 'comment')),
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    UNIQUE(document_id, user_id, team_id)
);
```

### 4.3 MinIO对象存储配置

- **存储桶设计**：
  - `note-gen-documents`：存储Markdown文档
  - `note-gen-attachments`：存储附件
  - `note-gen-vector-chunks`：存储向量分块原始内容

- **配置优化**：
  - 启用压缩，减少存储空间占用
  - 配置合理的生命周期策略，自动清理过期数据
  - 启用服务器端加密，确保数据安全

### 4.4 文档访问流程

1. 用户请求访问文档
2. 应用服务器验证用户权限
3. 从PostgreSQL获取文档元数据
4. 根据MinIO Key从MinIO获取文档内容
5. 返回文档内容给用户

## 5. 向量存储解决方案

### 5.1 向量数据库选型

**Qdrant Lite**：轻量级向量数据库，适合资源有限的环境

- **优势**：
  - 轻量级，占用资源少
  - 支持REST API，易于集成
  - 支持多种距离度量（包括余弦相似度）
  - 支持过滤和分页
  - 开源免费

### 5.2 向量数据模型设计

**集合设计**：
```
collection_name: note_vectors
vector_size: 1536  # 例如，OpenAI Ada模型的向量维度
distance: Cosine   # 余弦相似度
```

**向量点元数据**：
```json
{
  "id": "uuid",
  "vector": [0.1, 0.2, ..., 0.9],
  "payload": {
    "document_id": 123,
    "filename": "example.md",
    "chunk_id": 0,
    "content": "这是文档的一个分块内容",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
}
```

### 5.3 向量生成和更新流程

1. **文档创建/更新**：
   - 用户创建或更新文档
   - 应用服务器将文档内容保存到MinIO
   - 异步任务：将文档分割成多个块
   - 调用AI服务生成每个块的向量嵌入
   - 将向量存储到Qdrant

2. **向量查询流程**：
   - 用户发起向量查询
   - 应用服务器生成查询向量
   - 调用Qdrant API查询相似向量
   - 从结果中获取文档信息
   - 返回相似文档给用户

### 5.4 性能优化

- **向量维度优化**：选择合适的向量维度，平衡性能和精度
- **分块大小优化**：根据文档类型调整分块大小，提高查询相关性
- **异步处理**：向量生成和更新采用异步方式，避免阻塞主流程
- **缓存机制**：缓存频繁查询的向量结果，减少向量数据库访问

## 6. 资源有限情况下的优化策略

### 6.1 计算资源优化

- **容器化部署**：使用Docker容器化部署，提高资源利用率
- **资源限制**：为每个服务设置合理的CPU和内存限制
- **自动扩缩容**：根据负载自动调整服务实例数量
- **优化查询**：优化数据库和向量数据库查询，减少资源消耗

### 6.2 存储资源优化

- **数据压缩**：启用MinIO和PostgreSQL的数据压缩
- **数据归档**：定期归档不常用的文档和向量数据
- **向量量化**：使用向量量化技术，减少向量存储占用空间
- **增量更新**：只更新变化的文档和向量，避免全量更新

### 6.3 网络资源优化

- **CDN加速**：使用CDN加速静态资源访问
- **API缓存**：缓存频繁访问的API响应
- **批量操作**：合并多个请求为批量操作，减少网络开销

## 7. 部署与维护建议

### 7.1 部署方式

- **单服务器部署**：适合小型企业，所有服务部署在同一台服务器上
- **Docker Compose**：使用Docker Compose管理服务依赖和部署
- **简化配置**：移除不必要的服务和功能，只保留核心组件

### 7.2 维护建议

- **定期备份**：定期备份数据库和对象存储数据
- **监控告警**：使用简单的监控工具（如Prometheus + Grafana）监控系统状态
- **日志管理**：集中管理日志，便于排查问题
- **定期优化**：定期优化数据库和向量数据库，清理过期数据

### 7.3 安全建议

- **数据加密**：启用传输和存储加密
- **访问控制**：严格的权限控制，最小化权限原则
- **定期更新**：定期更新软件版本，修复安全漏洞
- **安全审计**：记录关键操作日志，便于安全审计

## 8. 与现有系统的兼容性

### 8.1 数据迁移

- **文档迁移**：编写脚本将现有Markdown文件迁移到MinIO
- **向量迁移**：将SQLite中的向量数据迁移到Qdrant
- **用户迁移**：支持从现有系统导入用户数据

### 8.2 API兼容性

- 保持核心API的兼容性，便于现有Web客户端迁移
- 提供API文档，便于集成和开发

## 9. 成本估算

### 9.1 硬件成本

- **服务器**：1-2台普通服务器（8核CPU，16GB内存，1TB存储）
- **网络**：普通企业宽带即可

### 9.2 软件成本

- **全部开源软件**：无 licensing 成本
- **维护成本**：1-2名兼职管理员即可维护

### 9.3 运营成本

- **电力成本**：较低，服务器功耗约100-200W
- **带宽成本**：根据实际使用情况而定

## 10. 总结

本解决方案针对资源有限的企业，设计了一个轻量级的文档和向量存储系统，具有以下特点：

- **轻量级架构**：只保留核心服务，减少资源占用
- **集中化管理**：企业数据集中存储，便于管理和安全控制
- **高性能**：使用专门的向量数据库，提高查询性能
- **低成本**：使用开源软件，降低硬件和维护成本
- **易于部署和维护**：适合资源有限的企业

该解决方案可以帮助资源有限的企业快速部署企业级文档和向量存储系统，满足企业的核心需求，同时控制成本和资源占用。